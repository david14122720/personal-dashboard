# Tasks: Finance Simplify + Movements (finance-simplify-movements)

- change: `finance-simplify-movements`
- date: 2026-09-24
- store: hybrid (this file is authoritative; mirrored to Engram `sdd/finance-simplify-movements/tasks`, project `personal-dashboard`)
- inputs: `proposal.md`, 14 spec files under `specs/`, `design.md` (per-slice file ownership, split points, merge order)
- delivery strategy: `auto-chain` · review budget: 400 changed lines per PR · chain strategy: `pending` (orchestrator collects it after this forecast — do NOT ask the owner from apply)
- threat matrix: N/A per design (no routing/shell/VCS boundary; adversarial surface is the movement/pay test matrix + the 0013 gate guard) — no RED-test tasks generated from it.

## Review Workload Forecast

| Slice | Estimated changed lines (add + del) | 400-line budget risk | Notes |
|-------|--------------------------------------|----------------------|-------|
| S-A BE movements core (A1+A2) | ~1000 | High | New `movements.rs` with full delta-reversal test matrix dominates |
| S-B BE subscriptions v2 | ~500 | High | `dates.rs` + widened PATCH + pay endpoint + cycle tests |
| S-C FE movements (C1+C2) | ~1350 | High | Two new components + hooks/transforms + MSW tests |
| S-D FE subscriptions + Settings | ~720 | High | New Settings section + form extraction + pay modal + tests |
| S-E FE categories/charts | ~610 | High | Chart re-point + compare split + localStorage v2 + tests |
| S-H Resumen D1 rework (H1+H2) | ~1320 | High | Strip + 2 new widgets + 4 cascade consumers + tests |
| S-F FE removals (delete-only) | ~1500 (mostly deletions) | High | Cognitive load is "any reference left?"; `size:exception` candidate |
| S-G BE destructive removal (GATED) | ~1400 (mostly deletions) | High | Route deletions dominate; migration itself is ~40 lines |
| **Total** | **~8400** | **High** | Honest estimate; exceeds 400 by an order of magnitude |

| Field | Value |
|-------|-------|
| Estimated changed lines | ~8400 (additions + deletions) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 (S-A) → PR 2 (S-B) → PR 3 (S-C) → PR 4 (S-D) → PR 5 (S-E) → PR 6 (S-H) → PR 7 (S-F) → PR 8 (S-G, gated) |
| Delivery strategy | auto-chain |
| Chain strategy | pending |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 (S-A1) | 0012 additive migration + ownership validators + shape guard | PR 1a | `cargo test --test migration_0012_movements` | N/A (DDL + helpers only, no user-visible behavior) | `backend/migrations/0012_movements.sql`, `backend/src/finance/validation.rs`, `backend/tests/migration_0012_movements.rs` — revertible by dropping `movements` + `last_paid_on` on dev DB |
| 2 (S-A2) | Movements CRUD + transactional balance + accounts 409 guard | PR 1b | `cargo test movements` | `curl POST /api/movements` expense → balance −amt; DELETE → balance restored | `backend/src/routes/movements.rs`, `backend/src/routes/mod.rs`, `backend/src/main.rs` (movements wiring), `backend/src/routes/accounts.rs` (delete tests) |
| 3 (S-B) | Subscription pay endpoint + widened PATCH + cycle math | PR 2 | `cargo test subscriptions` | `curl POST /api/subscriptions/{id}/pay` → debit + movement + `last_paid_on`; second pay → 409 | `backend/src/finance/dates.rs`, `backend/src/routes/subscriptions.rs`, pay wiring in `backend/src/main.rs` |
| 4 (S-C1) | Movement wire hooks + transforms + unit tests | PR 3a | `pnpm test lib/finance/finance.test.ts` | N/A (no UI yet; pure functions + hooks) | `frontend/lib/api/finance.ts` (movement part), `frontend/lib/finance/finance.ts` (movement part) |
| 5 (S-C2) | Add expense/income modals + history + Finance wiring + visual check | PR 3b | `pnpm test` (MovementForms/History suites) | Playwright (webapp-testing skill): add expense end-to-end → balance changes + history row appears; Esc/focus; zero console errors | `frontend/components/finance/MovementForms.tsx`, `frontend/components/finance/MovementHistory.tsx`, `FinanceScreens.tsx` add-shells, `FinanceSections.tsx` `onSelect`, movement i18n keys |
| 6 (S-D) | Settings subscription manager + Pay flow + visual check | PR 4 | `pnpm test` (subscriptions suites) | Playwright: create sub in Settings → pay in Finance → "Pagado este ciclo"; double-pay blocked message; zero console errors | `frontend/components/settings/SubscriptionsSection.tsx`, `SubscriptionForms.tsx` rework, `frontend/app/dashboard/ajustes/page.tsx`, pay/patch hooks, pay i18n keys |
| 7 (S-E) | Kind-free categories + split charts + compare + visual check | PR 5 | `pnpm test` (customCategories + charts + compare suites) | Playwright: category picker shows all kinds together; chart renders gasto+ingreso two-series; compare split without netting; zero console errors | `frontend/lib/settings/customCategories.ts`, `CustomCategoriesSection.tsx`, `CategoryCharts.tsx`, `compare/page.tsx`, chart i18n keys |
| 8 (S-H1) | Resumen strip + latest-5 + upcoming-subs + visual check | PR 6a | `pnpm test` (DashboardHome + widgets suites) | Playwright: Resumen shows 4 KPIs + «Últimos movimientos» (5) + «Próximas suscripciones»; simplified Finance renders Accounts/Subs/Assets/movements/chart, no tabs; zero console errors | `DashboardHome.tsx`, `MovementsSnapshot.tsx`, `UpcomingSubs.tsx`, `lib/api/dashboard.ts` layout, Resumen i18n keys |
| 9 (S-H2) | Notifications/score/reports cascade | PR 6b | `pnpm test` (notifications + transforms suites) | Playwright: notifications bell with no `/debts` request; `/progreso` finance from net worth only; `/reportes` without debt figure | `useNotifications.ts`, `GoalProgress.tsx`, `UpcomingPayments.tsx`, `transforms.ts` cascade part, `ProgressScreens.tsx`, `ReportsScreens.tsx` |
| 10 (S-F) | Delete-only FE removals (debts/savings) | PR 7 | `pnpm test && pnpm exec tsc --noEmit && pnpm build` | Playwright grep: zero requests to `/debts` or `/savings-goals`; zero imports of removed modules | Deleted modules + import cleanups (full file list in S-F tasks); revert restores the modules — parent tables still exist until S-G |
| 11 (S-G) | Gated destructive removal (migration + routes + docs) | PR 8 (GATED) | `cargo test --test migration_0013_gate && cargo test && pnpm test` | N/A post-apply beyond suite green + catalog inspection (no user-visible feature; data loss by design) | Code revert restores shape but NOT rows — point of no return; see hard gate below |

Merge order (binding): `S-A → S-B → S-C → S-D → S-E → S-H → S-F → S-G`. S-F is independent of the BE chain and may land any time before S-G. S-G merges last.

Global rules for every unit: no new dependencies (flag, do not install); Conventional Commits; tests + docs alongside behavior per `work-unit-commits`; do not break unmentioned functionality; NO commit, NO push, NO deploy — planning and local implementation only.

## Phase S-A: BE movements core (`0012` + CRUD + transactional balance)

Owner files: `backend/migrations/0012_movements.sql`, `backend/src/routes/movements.rs` (create), `backend/src/finance/validation.rs` (modify), `backend/src/routes/mod.rs` (modify), `backend/src/main.rs` (movements wiring only), `backend/src/routes/accounts.rs` (delete tests only), `backend/tests/migration_0012_movements.rs` (create).

### S-A1: Migration + validators + shape guard

- [x] A-1.1 Author `backend/migrations/0012_movements.sql`: `CREATE TYPE movement_direction AS ENUM ('expense','income')`; `CREATE TABLE movements` with columns `id UUID PK DEFAULT gen_random_uuid()`, `user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE`, `account_id UUID NOT NULL REFERENCES accounts(id) ON DELETE RESTRICT`, `category_id UUID REFERENCES categories(id) ON DELETE SET NULL`, `direction movement_direction NOT NULL`, `amount NUMERIC(18,2) NOT NULL CHECK (amount > 0)`, `occurred_on DATE NOT NULL`, `description TEXT`, `subscription_id UUID REFERENCES subscriptions(id) ON DELETE SET NULL`, `created_at/updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`; indexes `idx_movements_user_date (user_id, occurred_on DESC, created_at DESC, id DESC)`, `idx_movements_user_account (user_id, account_id)`, `idx_movements_user_category (user_id, category_id)`; `ALTER TABLE subscriptions ADD COLUMN last_paid_on DATE`. Acceptance: additive only — no `DROP`/`ALTER … DROP` of pre-existing objects; satisfies `finance-movements` "Movement Record Model" + `finance-core-invariants` "Migration Discipline" (0012 additive) and "Post-Migration Schema Shape" (0012 half).
- [x] A-1.2 Rewrite `backend/src/finance/validation.rs`: add `ensure_owned_category` (ownership only, no kind check) and `ensure_owned_account` (existence + ownership); remove `#[allow(dead_code)]` on `validate_occurred_on` (first live consumer arrives in A-2.1); remove kind checks from `ensure_finance_category` (and from `subscriptions.rs::ensure_subscription_category`, coordinated with S-B task B-2.1 — S-A owns the helper shape, S-B owns the subscription call-site removal). Acceptance: `finance-core-invariants` "Shared Finance Validation Helper Ownership" — helpers live in surviving `finance/validation.rs`, foreign/missing category → 422 Spanish, any owned kind accepted.
- [x] A-1.3 Create `backend/tests/migration_0012_movements.rs`: file-content guard (0012 exists; contains only `CREATE TABLE`/`CREATE TYPE`/`CREATE INDEX`/`ADD COLUMN` tokens, no `DROP`/`CASCADE`/`BACKUP`/`DUMP`); live shape test (skip without `DATABASE_URL`): `movements` with columns/checks/FKs/3 indexes, `subscriptions.last_paid_on` exists, no trigger writes `accounts.balance`, migrations `0001`–`0011` byte-identical guard passes. Acceptance: `finance-core-invariants` "Migration Discipline" (0012 reviewable) + "Post-Migration Schema Shape" + "Movement Balance Write Discipline" (no trigger).

### S-A2: Routes + transaction + accounts guard

- [x] A-2.1 Create `backend/src/routes/movements.rs`: `CreateMovementRequest` (exactly `direction, amount, account_id, category_id, occurred_on, description?`, `deny_unknown_fields`) and `PatchMovementRequest` (same fields all-optional, `subscription_id` rejected as unknown); `MovementResponse` (amount `Decimal` → decimal string, `occurred_on` `NaiveDate` → `YYYY-MM-DD`); handlers `create/list/get/patch/delete`; `validate_direction` (`expense|income` else Spanish 422); `validate_movement_amount` (`parse_money_amount` + scale ≤ 2 + < 10^6); `map_movement_db_err` (`23514|23503|22P02 → 422`, else Internal). Transaction shape per design §Data Flow: `SELECT … FOR UPDATE` lock-first on the account, `ensure_owned_category`, `INSERT`, `UPDATE accounts SET balance = balance ± amt`; PATCH locks old+new accounts in ascending UUID order, reverses stored signed delta then applies new; DELETE returns the row then reverses. Status matrix: 401 no token; 404 URL-addressed foreign/missing movement; 422 referenced foreign/missing `account_id`/`category_id`, unknown/missing/malformed fields; 409 only via accounts delete. Acceptance: `finance-movements` "Movement REST Contract" (all six scenarios incl. create-expense 201 `"25000.00"`, number-amount/`subscription_id`/bad-date 422, card-income 201, foreign 404, PATCH `subscription_id` 422, 401) + "Atomic Balance Effect" (insert/delete/edit-reversal/no-partial-effect/no-trigger).
- [x] A-2.2 Wire movements in `backend/src/routes/mod.rs` (`pub mod movements;`) and `backend/src/main.rs` (mount `GET/POST /api/movements`, `GET/PATCH/DELETE /api/movements/{id}`; extend the wiring test for the new routes only — subscription-pay wiring belongs to S-B, removal assertions to S-G). Acceptance: `finance-core-invariants` "Movement And Removal Chain Ordering" (S-A atomic: migration + routes + balance mechanics together).
- [x] A-2.3 Rewrite the removed-table-seeding delete test in `backend/src/routes/accounts.rs`: replace `delete_account_with_live_finance_rows_is_204` (which seeds removed tables) with `delete_account_without_movements_is_204` (204, gone) + `delete_account_with_movements_is_409` (409 Spanish, account + balance unchanged); keep `23503 → 409` mapping and foreign-id 404. Acceptance: `finance-movements` "Account Delete Guard Blocks Referenced Accounts" (all four scenarios) + `finance-accounts` "Account Delete Guard Uses Live References Only" (204/409/23503-mapping/foreign-404/typed-FE-key — the FE half lands in S-C) + `finance-core-invariants` "Hermetic Tests For Surviving Modules" (no test seeds a removed table).
- [x] A-2.4 DB-gated movement tests in `backend/src/routes/movements.rs` (`#[tokio::test]`, skip without `DATABASE_URL`, `db_state`/`cleanup_user` helpers, fixtures only through surviving tables/API): full delta-reversal matrix (POST expense/income, PATCH amount-only/direction-flip/account-move/combo, DELETE expense/income), rollback-leaves-nothing, concurrent same-account serialization, 404-vs-422 matrix, account deleted mid-transaction → Spanish 4xx with nothing persisted. Acceptance: design Testing Strategy (backend DB-gated row) + `finance-movements` "Atomic Balance Effect" scenarios.

Per-slice verification (S-A): `cd backend && cargo test` (unit + DB-gated with local DB at 0012; file-content guards run without DB). Rollback boundary: `backend/migrations/0012_movements.sql` + `backend/src/routes/movements.rs` + `backend/src/finance/validation.rs` + `backend/src/routes/mod.rs` + movements wiring in `backend/src/main.rs` + `backend/src/routes/accounts.rs` delete tests + `backend/tests/migration_0012_movements.rs` — revert the commits; 0012 is additive, revertible by dropping `movements` + `last_paid_on` via a fresh follow-up migration or by discarding local dev DB state (no production apply authorized).

## Phase S-B: BE subscriptions v2 (pay endpoint + widened PATCH + cycle math)

Owner files: `backend/src/finance/dates.rs` (create), `backend/src/routes/subscriptions.rs` (modify), `backend/src/main.rs` (pay wiring only), subscription tests.

- [x] B-1.1 Create `backend/src/finance/dates.rs`: `today_bogota()` (`Utc::now() - 5h → date_naive`; pure-function seam with injected `now` for tests), `add_months_clamped(base, k)` (month-index arithmetic, `day = min(day, last_day)` — `2026-01-31 → 2026-02-28 → 2026-03-31`, no drift), `advance_next_billing(stored, pay_date)` (NULL → pay + 1 month; else anchor day from stored, first `candidate_k > max(pay_date, stored)`, bounded 1200 iterations, fallback pay + 1 month). Unit tests for all five cycle scenarios. Acceptance: `finance-subscriptions` "Subscription Cycle State" (due-date-arrival, early-payment-counts, month-end-clamp, long-dormant-jump, null-due) + design Decision "Cycle math" (`max(pay_date, stored_due)` threshold).
- [x] B-2.1 Modify `backend/src/routes/subscriptions.rs`: add `last_paid_on: Option<NaiveDate>` to row/response (tuple 14 → 15); `CreateSubscriptionRequest.frequency` → `Option<String>` (absent or `"monthly"` accepted, anything else 422), `next_billing_on` required; `PatchSubscriptionRequest` exactly `name|price|next_billing_on|is_active` with `deny_unknown_fields`, single-statement `COALESCE` + server-owned `cancelled_at` `CASE`; remove kind gate (`ensure_subscription_category` → shared `ensure_owned_category`); add `PaySubscriptionRequest { account_id }` + `pay_subscription_handler` per design §Data Flow (lock account then subscription `FOR UPDATE`, guards inactive → 422 / price 0 → 422 / paid-this-cycle → 409 `finance.subscriptionPaidThisCycle`, idempotent `Suscripciones` seed via `ON CONFLICT (user_id,kind,name) DO UPDATE … RETURNING id`, insert expense movement with `today_bogota`, debit, stamp + advance, 200 full subscription). Foreign subscription/account → 404 (never 403). Acceptance: `finance-subscriptions` "Subscription Management" (create/monthly-force/no-due-422), "Lifecycle Management" (cancelled not payable 422), "Category Scoping" (any owned kind accepted, foreign 422), "Ownership & Security" (foreign pay 404), "Subscription Cancel and Reactivate" (allowlist + non-allowlist 422), "Subscription Pay Action" (all six scenarios), "Fixed Suscripciones Category Seed" (idempotent/other-kind-untouched/seeded-id/normal-category), "Subscription Manual ES COP Contract" (BE half: monthly wire, string money).
- [x] B-2.2 Wire `POST /api/subscriptions/{id}/pay` in `backend/src/main.rs` + extend the wiring test for the pay route. Acceptance: pay reachable; no other wiring touched.
- [x] B-2.3 DB-gated subscription tests (skip without `DATABASE_URL`): pay debits atomically + single audit movement with seeded category link + `last_paid_on = today`; double-pay 409 with no movement and unchanged balance; free (`0.00`) 422; inactive 422; foreign sub/account 404; pay-error rollback (nothing persisted); seed idempotency + other-kind collision untouched; widened PATCH accepts only the four fields; kind-free acceptance; delete preserves movements via `ON DELETE SET NULL`. Acceptance: design Testing Strategy (backend DB-gated row, pay half).

Per-slice verification (S-B): `cd backend && cargo test` (unit incl. `dates.rs` + DB-gated with local DB at 0012). Rollback boundary: `backend/src/finance/dates.rs` + `backend/src/routes/subscriptions.rs` + pay wiring in `backend/src/main.rs` — revert leaves S-A intact; no migration involved.

## Phase S-C: FE movements (hooks/transforms + modals + history)

Owner files: `frontend/lib/api/finance.ts` (movement hooks + savings `createMovement`/`deleteMovement` rename), `frontend/lib/finance/finance.ts` (movement transforms/dates), `frontend/components/finance/MovementForms.tsx` + `frontend/components/finance/MovementHistory.tsx` (create), `frontend/components/containers/FinanceScreens.tsx` (add shells + filter state), `frontend/components/finance/FinanceSections.tsx` (`AccountsList` select), movement i18n keys in `frontend/lib/i18n/es.ts`, movement tests.

### S-C1: Hooks + transforms + unit tests

- [x] C-1.1 Extend `frontend/lib/api/finance.ts`: `MovementWire` (decimal-string amount, `YYYY-MM-DD` date), `useMovements` (SWR key `finance/movements`, `GET /movements`, no pagination), `createMovement` (decimal-string amount), `patchMovement`, `deleteMovement`; rename the savings hooks currently colliding on `createMovement`/`deleteMovement` to savings-specific names (they are deleted in S-F; the rename only resolves the dead collision). `useCategories` drops the kind param (single unfiltered `GET /categories`, key `finance/categories`); add `paySubscription`/`patchSubscription` thin wrappers here if S-D prefers them co-located (S-D owns their tests). Acceptance: `finance-movements` "Movement History List" (SWR scope + refresh) + "Add Expense And Income UI" (decimal-string POST) + `frontend-dashboard` "Kind-Free Category UI" (single unfiltered set).
- [x] C-1.2 Extend `frontend/lib/finance/finance.ts`: `toMovementRows` (direction/amount via existing money formatter `es-CO`/`COP`, Spanish date, category/account names, description passthrough), `toCategoryMovementTotals(movements, currencyByAccountId, userCurrency, categoryId)` (`{expense, income}`, null-category excluded, non-user-currency excluded never converted, never netted), `toTotalBalance` (Σ `accounts.balance` same-currency only), `todayInBogota` (`Intl en-CA` + `America/Bogota`), `isPaidThisCycle` (render-derived, no effects/timers). Vitest suites for: two-series vs single-series, no-netting, null-category exclusion, foreign-currency exclusion, `toTotalBalance` currency filter, `todayInBogota`, paid derivation. Acceptance: `finance-movements` "Movement Category Aggregation" (all four scenarios) + `frontend-dashboard` "Telemetry Strip" (total-balance currency rule) + direction-based fixtures only (`finance-core-invariants` "Frontend Test Suite Alignment").

### S-C2: Modals + history + screen wiring + visual check

- [x] C-2.1 Create `frontend/components/finance/MovementForms.tsx` (`MovementModal` add/edit): fields Amount (required, decimal-string, ≤ 2 decimals client check), Account (required, by name), Category (required, unfiltered all-owned), Date (required `YYYY-MM-DD`, default today), Description (optional); Spanish client validation blocking the request; CSS-only success confirmation (no toast lib); Esc/cancel closes with no request and focus returns to the opener; delete-with-confirmation affordance; ≥44px targets, keyboard reachable, all copy from typed `finance.*` keys. MSW tests: validation blocks request; success posts decimal string + revalidates `finance/movements`; Esc restores focus. Acceptance: `finance-movements` "Add Expense And Income UI" (all four scenarios) + "Movement REST Contract" (FE half).
- [x] C-2.2 Create `frontend/components/finance/MovementHistory.tsx`: latest-50 in API order, row = direction/amount/date/category/account/description; filters account/category/direction (composable); account-click filter set from `FinanceScreens` state, visible + clearable; independent Spanish loading/error(`retry` revalidates only `finance/movements`)/empty; `content-visibility: auto` on rows; edit/delete affordances wired to `patchMovement`/`deleteMovement`. MSW tests: order + 50-cap, filter composition, error/empty independence. Acceptance: `finance-movements` "Movement History List" (all four scenarios) + "Add Expense And Income UI" edit/delete scenario.
- [x] C-2.3 Modify `frontend/components/containers/FinanceScreens.tsx` (add expense/income shells + history shell + account-click filter state; subscription-pay wiring belongs to S-D, chart props to S-E, shell deletions to S-F) and `frontend/components/finance/FinanceSections.tsx` (`AccountsList` gains `onSelect`/`activeAccountId` only). Add movement i18n keys per design §Interfaces (movements family; reuse existing `amountCop/dateLabel/category/selectAccount/save/cancel` keys where they fit). 409 account-delete surfaces `finance.accountDeleteBlocked` and keeps the account listed (FE half of `finance-accounts` guard). Acceptance: `frontend-dashboard` "Finance Single-View Composition" (single grid, no tabs, equal cards — S-C contributes the movements shells) + `finance-accounts` "Account Delete Guard" FE scenario.
- [ ] C-2.4 Playwright visual check (project `webapp-testing` skill): add expense/income end-to-end — submit expense → account balance changes by the signed delta and history row appears; filter by account via card click; Esc closes modal with focus restored; zero console errors. Record viewport(s) + exact result in the slice evidence.

Per-slice verification (S-C): `cd frontend && pnpm test && pnpm exec tsc --noEmit && pnpm build` + C-2.4 Playwright evidence. Rollback boundary: `frontend/lib/api/finance.ts` (movement part + rename), `frontend/lib/finance/finance.ts` (movement part), `MovementForms.tsx`, `MovementHistory.tsx`, Finance add/history shells + `AccountsList` select, movement i18n keys — revert removes the movements UI; backend S-A stays mounted but unread.

## Phase S-D: FE subscriptions + Settings (manager + Pay flow)

Owner files: `frontend/components/settings/SubscriptionsSection.tsx` (create), `frontend/components/finance/SubscriptionForms.tsx` (modify: extract form, Pay row/modal), `frontend/app/dashboard/ajustes/page.tsx` (modify), `frontend/lib/api/finance.ts` (pay/patch hooks — if placed in C-1.1, owned here for tests), pay/settings i18n keys, subscription FE tests.

- [x] D-1.1 Create `frontend/components/settings/SubscriptionsSection.tsx`: list + create/edit with name, price (decimal string, client validation, Spanish error, no request on invalid), monthly due date `YYYY-MM-DD` (required), category by name, payment method (incl. "Transferencia" via preserved `finance.paymentTransfer` key); NO frequency selector; create sends `POST /subscriptions` without frequency, edit sends widened `PATCH` with allowlisted fields only; refresh `finance/` SWR scope; typed `finance.subscriptionSettings*`/`finance.subscriptionDue*` keys. Tests: create/edit payloads, invalid-price block, no-frequency-control assertion. Acceptance: `finance-subscriptions` "Subscription Create Form" (all four scenarios) + "Payment Method Catalog Stability" (Transferencia present + stored value renders) + "Subscription Manual ES COP Contract" (manual/Spanish/COP/monthly-fixed, no frequency UI).
- [x] D-1.2 Modify `frontend/components/finance/SubscriptionForms.tsx`: extract the create/edit form to Settings (D-1.1); `SubscriptionRow` keeps cancel/reactivate/delete (widened `PATCH`, explicit confirm for delete) and gains the Pay button + account-pick modal (sends only `{account_id}` to `POST /{id}/pay`, CSS-only confirmation, revalidates subscription row + account card) + "Pagado este ciclo" paid state (render-derived via `isPaidThisCycle`, no effects); hide Pay when `price = 0.00`; 409 double-pay surfaces `finance.subscriptionPaidThisCycle` in Spanish; movements survive delete (`ON DELETE SET NULL` audit — FE removes the row locally). Render `SubscriptionsSection` in `frontend/app/dashboard/ajustes/page.tsx`. Tests: pay modal payload, double-pay 409 message, free-row no-Pay, cancel/reactivate payloads, delete removes row. Acceptance: `finance-subscriptions` "Subscription Cancel and Reactivate" (FE scenarios), "Subscription Delete" (both scenarios), "Subscription Contract Preserved" (FE halves: 404 + 409 in Spanish, no invented endpoint), "Subscription Manual ES COP Contract" ("List shows amount and paid state", "No frequency control"), "Subscription Pay Action" pay-modal scenario.
- [x] D-1.3 Playwright visual check (project `webapp-testing` skill): full subscription pay flow — create subscription in Settings (name/price/due, no frequency control visible) → Finance row shows COP price + due → Pay → pick account → confirm → "Pagado este ciclo" appears and account balance drops by price → second Pay blocked with Spanish message; zero console errors. Record viewports + exact result.

Per-slice verification (S-D): `cd frontend && pnpm test && pnpm exec tsc --noEmit && pnpm build` + D-1.3 Playwright evidence. Rollback boundary: `SubscriptionsSection.tsx` + `SubscriptionForms.tsx` rework + `ajustes/page.tsx` + pay/patch hooks + pay/settings i18n keys — revert restores the Finance-resident form; S-B endpoint stays mounted but uncalled.

## Phase S-E: FE categories/charts (kind-free UI + movement aggregates)

Owner files: `frontend/lib/settings/customCategories.ts` + `frontend/components/settings/CustomCategoriesSection.tsx` (modify), `frontend/components/finance/CategoryCharts.tsx` (modify), `frontend/app/dashboard/finance/compare/page.tsx` (modify), `frontend/components/containers/FinanceScreens.tsx` (single unfiltered category set), chart i18n keys, category/chart tests.

- [x] E-1.1 Modify `frontend/lib/settings/customCategories.ts`: versioned envelope `{ v: 2, categories: [{id, name}] }` on key `pd-custom-categories`; reader accepts legacy array shape (`{id, name, kind}`) and migrates in place (id/name preserved, kind dropped, write-back v2); fix the stale docstring; new `customCategories.test.ts` (legacy→v2 lossless, v2 round-trip). Modify `CustomCategoriesSection.tsx`: drop kind select + kind badge; copy states customs stay localStorage-only and out of API selects. Acceptance: `frontend-dashboard` "Kind-Free Category UI" (no-kind-control, schema-migration, DB-untouched scenarios) + vercel-react `client-localstorage-schema` rule.
- [x] E-1.2 Modify `frontend/components/finance/CategoryCharts.tsx`: `CategoryBars` becomes a two-series div-bar composition (gasto/ingreso per category from `toCategoryMovementTotals`; a series mounts only when its aggregated value > 0); `CategoryChartSection` reads `GET /movements` (same `finance/movements` SWR key, no new endpoint); theme tokens only, Spanish labels, COP tooltips, `prefers-reduced-motion` suppression; rewritten `finance.chart*` keys. Pass the single unfiltered category set through `FinanceScreens.tsx`. Tests: two-series/single-series, no-netting, no hex, no kind-split. Acceptance: `frontend-dashboard` "Finance Category Chart And Compare Split" (chart scenarios) + "Retained Chart Contracts" (div-bar, no new dep, token-only) + `finance-movements` "Movement Category Aggregation" (chart half).
- [x] E-1.3 Modify `frontend/app/dashboard/finance/compare/page.tsx`: per-category expense/income side-by-side from the same aggregate (no netting, no currency mixing, unfiltered categories, movement source only); stays a Finance sub-route off the primary nav. Tests: split figures, no netted figure, COP-only exclusion. Acceptance: `frontend-dashboard` "Finance Category Chart And Compare Split" (compare + no-currency-mixing scenarios) + "Finance Single-View Composition" (compare-reachable scenario).
- [x] E-1.4 Playwright visual check (project `webapp-testing` skill): kind-free categories (picker lists `finance`+`subscription`+`habit` kinds together, no badge/selector anywhere) + split charts (category with both directions shows gasto + ingreso bars; expense-only shows one series) + compare split; zero console errors. Record viewports + exact result.

Per-slice verification (S-E): `cd frontend && pnpm test && pnpm exec tsc --noEmit && pnpm build` + E-1.4 Playwright evidence. Rollback boundary: `customCategories.ts` + section + `CategoryCharts.tsx` + `compare/page.tsx` + unfiltered-set wiring + chart i18n keys — revert restores kind UI and the proxy chart; legacy localStorage shape still readable via the v2 reader (no data loss either direction).

## Phase S-H: Resumen D1 rework (strip + sections + cascade)

Owner files: `frontend/components/containers/DashboardHome.tsx` (modify), `frontend/components/dashboard/widgets/MovementsSnapshot.tsx` + `UpcomingSubs.tsx` (create), `frontend/components/dashboard/widgets/{GoalProgress,UpcomingPayments}.tsx` (modify), `frontend/lib/dashboard/transforms.ts` + `frontend/lib/api/dashboard.ts` (modify), `frontend/components/notifications/useNotifications.ts` (modify), `frontend/components/containers/{ProgressScreens,ReportsScreens}.tsx` (modify), Resumen i18n keys, Resumen/cascade tests.

### S-H1: Strip + sections + widgets

- [x] H-1.1 Modify `frontend/components/containers/DashboardHome.tsx`: 4-KPI strip (net worth, non-archived account count, monthly active-sub cost, total balance = Σ same-currency `accounts.balance`; debts/savings KPIs gone, no placeholder); mount «Últimos movimientos» (latest 5 from `finance/movements`, direction/COP/Spanish date/category/account, link to Finance) and «Próximas suscripciones» (active, `next_billing_on ≥ today` ascending, top 5, name/COP/due + paid state); remove the `pending-debts` widget block + customize row; independent Spanish loading/error/empty per section (retry revalidates only its own SWR keys). Tests: 4-KPI render, debts/savings absence + no `/debts`|`/savings-goals` request, total-balance currency filter, latest-5 order, upcoming-subs filter/order, empty/error independence. Acceptance: `frontend-dashboard` "Telemetry Strip" (all five scenarios) + "Resumen Latest Movements And Upcoming Subscriptions Sections" (all three) + "Finance Screen Source Integrity" (no perpetual empty block).
- [x] H-1.2 Create `frontend/components/dashboard/widgets/MovementsSnapshot.tsx` and `UpcomingSubs.tsx` per H-1.1 contracts; modify `GoalProgress.tsx` («Metas» from `GET /goals` only, «Ahorro» segment + `useSavingsGoals` gone, metas-only copy) and `UpcomingPayments.tsx` (union subs + `payment_due` events, tie-break events > subs, 7-day inclusive window, no debts fetch); `frontend/lib/api/dashboard.ts` default layout → 5 widgets `(20, 22, 23, 24, 30)`; stale `pending-debts`/`month-*` entries ignored silently (no block, no fetch, no error). Tests incl. `DashboardHome.widgets.test.tsx` rewrite: 5-widget sweep, no `pending-debts`, stale-entry silence, hide-stops-fetch, round-trip persistence, invalid-layout fallback. Acceptance: `dashboard-widgets` "Upcoming Payments 7-Day Union", "Goal Progress", "Customization and Persistence" (all scenarios), "Composition Constraints" (no removed dependency), "Pending Debts List" REMOVED.
- [x] H-1.3 Playwright visual check (project `webapp-testing` skill): Resumen renders 4 KPIs + both sections with real data; widgets sweep covers exactly the 5 survivors; no `pending-debts` block; zero console errors. Record viewports + exact result.

### S-H2: Notifications / score / reports cascade

- [x] H-2.1 Modify `frontend/components/notifications/useNotifications.ts`: `kind ∈ task|subscription|event` (`debt` gone); «Vencidas» = overdue tasks + `payment_due` events past; «Próximos cobros» = updated `upcoming-payments` union + same window/order; mute examples re-keyed off `pending-debts` (stale debt mutes inert); rewrite `notifications.test.tsx` (overdue derivation, union equality, window borders, no `/debts` request, inert debt mute). Acceptance: `notifications` "Notification Item Contract" + "Mute Semantics" (all scenarios).
- [x] H-2.2 Modify `frontend/lib/dashboard/transforms.ts` (strip/score/snapshot rework: `toFinanceScore` net-worth-only renormalized 100/0/null, `toPendingDebts`/`toOutstandingDebt`/`toTotalSavings` deleted, savings segment removed; union minus debts; `toCategoryTotals`/`toDebtRows`/`toSavingsViews` deletion belongs to S-F — do NOT delete them here, only stop calling them from touched paths), `frontend/components/containers/ProgressScreens.tsx` (finance area = net worth only, score renormalized, no `/debts`|`/savings-goals`), `frontend/components/containers/ReportsScreens.tsx` (outstanding-debt figure + debt read removed; net worth + sub cost survive). Rewrite `transforms.test.ts` (score renormalization incl. negative→0 and no-data→null, no removed-figure assertions). Acceptance: `progress-score` "Combined Progress Dashboard" + "Visual Area Score", `reports-screen` "Finance Snapshot Block From Surviving Sources" (all scenarios), `dashboard-widgets` "Widget Loading Error and Empty States" (no permanent empty artefact).

Per-slice verification (S-H): `cd frontend && pnpm test && pnpm exec tsc --noEmit && pnpm build` + H-1.3 Playwright evidence. Rollback boundary: H1 = `DashboardHome.tsx` + 2 new widgets + `GoalProgress`/`UpcomingPayments` + `dashboard.ts` layout + Resumen keys; H2 = `useNotifications.ts` + `transforms.ts` cascade + `ProgressScreens.tsx` + `ReportsScreens.tsx` + cascade tests — H1/H2 revert independently; modules deleted in S-F still exist so nothing dangles.

## Phase S-F: FE removals, delete-only (debts/savings shells + hooks + transforms + keys)

Owner files (delete/modify): `frontend/components/finance/{SavingsForms,DebtPayments}.tsx` (delete), `frontend/components/dashboard/widgets/PendingDebts.tsx` (delete), `frontend/components/containers/FinanceScreens.tsx` (remove debts/savings shells + S5 blocks), `frontend/components/finance/{FinanceSections,SubscriptionForms}.tsx` (remove savings/debt remnants), `frontend/lib/api/finance.ts` (delete debts/savings hooks incl. renamed savings movement hooks), `frontend/lib/finance/finance.ts` (delete `toCategoryTotals`/`toDebtRows`/`toSavingsViews`/`toDebtProgress`), `frontend/lib/api/dashboard.ts` (delete debt hooks/keys), `frontend/lib/dashboard/transforms.ts` (delete removed transforms — S-H only stopped calling them), `frontend/lib/i18n/es.ts` (delete savings/debts families per design §Interfaces), `frontend/e2e/{sections,dashboard-widgets,notifications}.spec.ts` (sweep to survivors), test rewrites: `lib/finance/finance.test.ts`, `lib/dashboard/transforms.test.ts`, `lib/api/dashboard.test.ts`, `DashboardHome.test.tsx`, `DashboardHome.widgets.test.tsx`, `notifications.test.tsx`, `components/finance/finance.test.tsx`.

- [x] F-1.1 Delete whole modules: `frontend/components/finance/SavingsForms.tsx`, `frontend/components/finance/DebtPayments.tsx`, `frontend/components/dashboard/widgets/PendingDebts.tsx`. Remove their imports/exports/routes wherever referenced. Acceptance: `finance-debts` REMOVED ("Debt Payments UI"), `finance-savings` REMOVED ("Savings Forms"), `dashboard-widgets` "Pending Debts List" REMOVED + "Composition Constraints" (no removed dependency).
- [x] F-1.2 Strip Finance shells: remove debts/savings shells + S5 write-sections from `FinanceScreens.tsx`; remove `SavingsList` from `FinanceSections.tsx` and any savings/debt remnant in `SubscriptionForms.tsx` (form remnant after S-D extraction); delete debts/savings hooks from `frontend/lib/api/finance.ts` and debt hooks/keys from `frontend/lib/api/dashboard.ts`. Acceptance: `frontend-dashboard` "Finance Single-View Composition" (removed-sections-absent), "Retained Chart Contracts" (old `CategoryBars` proxies `chartExpenses`/`chartSavings` gone), "Finance Screen Source Integrity" (deleted chain complete: hook + transform + keys together).
- [x] F-1.3 Delete dead transforms + keys: `toCategoryTotals`/`toDebtRows`/`toSavingsViews`/`toDebtProgress` (+ `toPendingDebts`/`toOutstandingDebt`/`toTotalSavings` if S-H2 left them), MSW savings/debts handlers with their consumers, i18n families `finance.debts*`, `finance.savings*`, `finance.manageDebts*`, `finance.manageSavings*`, `finance.deposit*`, `finance.withdraw*`, `finance.overWithdrawal*`, `finance.overPayment*`, `finance.paymentHistory*`, `finance.creditor*`, `finance.installment`, `finance.interestRate`, `finance.confirmDeleteGoal`, `finance.confirmDeletePayment`, `finance.correctPayment`, `finance.goalName`* (*only if no surviving consumer), `finance.savedPct`, `finance.completedBadge`, `finance.noDebts*`, `finance.noSavings*`, `finance.noSubscriptionCategories`, `finance.frequency`, `dashboard.pendingDebts*`, `dashboard.savingsSegment`, `dashboard.noFlowData`, `dashboard.noFlowHint`; rewrite `finance.subtitle`, `dashboard.overviewSubtitle`, `dashboard.goalProgressHint`/`goalVsSavings`, `finance.categoryChartHint`, `finance.chartEmptyHint`; preserve `finance.paymentTransfer`. Acceptance: `frontend-i18n` "Removed Feature Key Hygiene" (all five scenarios incl. unknown-key-fails-build + Transferencia preserved).
- [x] F-1.4 Rewrite the removed-table tests in their subject slices (consumer-of-removed-symbols suites): `lib/finance/finance.test.ts`, `lib/dashboard/transforms.test.ts`, `lib/api/dashboard.test.ts`, `DashboardHome.test.tsx`, `DashboardHome.widgets.test.tsx`, `notifications.test.tsx`, `components/finance/finance.test.tsx` — delete or rewrite against surviving behaviour; movement/chart tests key on `(category, direction)`; update `frontend/e2e/{sections,dashboard-widgets,notifications}.spec.ts` to surviving sections/widgets only (5 widgets, no savings/debts). Acceptance: `finance-core-invariants` "Frontend Test Suite Alignment" (all four scenarios) + "Removal Sequencing Without Dangling References" (frontend-green + no-UI-reads-removed-route halves).

Per-slice verification (S-F): `cd frontend && pnpm test && pnpm exec tsc --noEmit && pnpm build` + Playwright grep evidence: zero requests to `/debts`|`/savings-goals`, zero imports of removed modules. Rollback boundary: the deleted files + import cleanups + key deletions + test rewrites listed above — revert restores every module; parent tables still exist (S-G not yet applied), so nothing is unrecoverable. If this slice exceeds 400 changed lines (likely — delete-only), escalate `size:exception` explicitly with this cognitive-load rationale; NEVER self-accept.

## Phase S-G: BE destructive removal, GATED (0013 + routes + SQL + MCP + docs)

> **HARD GATE (restated, binding): migration `0013_remove_savings_debts.sql` MUST be authored at `openspec/changes/finance-simplify-movements/migrations/0013_remove_savings_debts.sql` — it MUST NEVER live in `backend/migrations/` while gated, and it MUST NEVER be applied (local dev DB, CI, or production) without (1) explicit owner authorization at apply time AND (2) deploy-gate re-confirmation of the intentional data loss (savings goals, goal movements, debts, debt payments — no backup/dump per standing decision). CI applies every `backend/migrations/*.sql` automatically and the compose DB mounts that folder as `docker-entrypoint-initdb.d`; placing 0013 there pre-gate would silently destroy rows. The copy into `backend/migrations/` happens ONLY at the authorized gate step, after every reversible slice has merged and every reference to the removed routes/tables is gone. Post-0013 there is NO data rollback by design.**

Owner files: `openspec/changes/finance-simplify-movements/migrations/0013_remove_savings_debts.sql` (stage — create here), `backend/src/routes/{savings,debts}.rs` (delete), `backend/src/routes/mod.rs` + `backend/src/main.rs` (removal + removal assertions), `backend/src/routes/events.rs` (modify: `debt_id` removal), `backend/src/routes/assets.rs` (modify: net-worth SQL + seed rewrite), `backend/tests/migration_0013_gate.rs` (create), `mcp-dashboard/src/tools.ts` + `mcp-dashboard/README.md` (modify), `objetivo.md` (modify).

- [x] G-1.1 Stage `openspec/changes/finance-simplify-movements/migrations/0013_remove_savings_debts.sql` (change folder ONLY — verify `backend/migrations/0013*` does NOT exist) with statements in 0011 order: (a) `ALTER TABLE events DROP COLUMN debt_id;` (inbound FK on a surviving table); (b) `DROP TABLE debt_payments;` + `DROP TABLE savings_goal_movements;` (children first); (c) `DROP TABLE debts;` + `DROP TABLE savings_goals;` (parents); (d) `DROP FUNCTION update_debt_pending();` + `DROP FUNCTION update_savings_goal_saved();`. No `CASCADE`, no `CREATE`, no data migration, no backup, no compatibility view. Acceptance: `finance-core-invariants` "Migration Discipline" (0013 removal-only) + "Gated Destructive Migration 0013" (file-exists-but-not-applied + no-automatic-application) + `finance-debts`/`finance-savings` Migration halves.
- [x] G-1.2 Create `backend/tests/migration_0013_gate.rs`: RED guard asserting 0013 exists in the change folder AND NOT in `backend/migrations/` pre-gate; removal-only token check (only `DROP`/`ALTER … DROP COLUMN`, no `CASCADE`/`CREATE`/`BACKUP`/`DUMP`); gate-order record (apply-time authorization + deploy re-confirmation named in order). Post-apply flip (documented in-file, executed only at the gate): catalog asserts the four tables + both functions absent, `events.debt_id` absent, surviving triggers/columns untouched. Acceptance: `finance-core-invariants` "Gated Destructive Migration 0013" (all three scenarios) + "Post-Migration Schema Shape" (0013 half) + "No-Backup And No-Dual-Read Decision".
- [x] G-2.1 Delete `backend/src/routes/savings.rs` + `backend/src/routes/debts.rs`; remove their mounts + modules in `backend/src/routes/mod.rs` + `backend/src/main.rs`; add removal assertions to the wiring test (routes gone, movements + pay still mounted). Acceptance: `finance-debts` REMOVED (routes/consumers) + `finance-savings` REMOVED (routes/consumers) + `finance-core-invariants` "Removal Sequencing" (backend-green + dead-references-removed halves).
- [x] G-2.2 Modify `backend/src/routes/events.rs`: drop the `events.debt_id` FK column read/write (DTO field, SQL columns/binds) and the `validate_link_ids` debt arm; rewrite the `events.rs` link-validation tests that seed `debts` to use surviving link targets only. Acceptance: design Open Question "Discovered cascade" closed — `DROP TABLE debts` has no surviving referent; `finance-core-invariants` "Hermetic Tests" (events tests seed no removed table).
- [x] G-2.3 Modify `backend/src/routes/assets.rs`: remove the active-debts leg from `NET_WORTH_SQL` (join assets + credit-card accounts only); redefine per-currency `debts` as card-only `GREATEST(-balance,0)` over non-archived `credit_card` accounts, keeping the `{currency, assets, debts, net_worth}` wire shape; rewrite the net-worth test seeds that insert into removed tables (arrange balances via direct column updates on surviving tables). Acceptance: `finance-assets` "Net Worth Aggregation" (all four scenarios) + "Net Worth Number" (FE halves verified in S-C/S-H) + hermetic seeds.
- [x] G-2.4 Modify `mcp-dashboard/src/tools.ts` (remove `list_debts`; no movements tool added; surviving tools byte-identical incl. names/schemas/auth/scopes) + `mcp-dashboard/README.md` (list survivors only; record transaction/budget parent removal + `list_debts` removal by this change + gated 0013). Acceptance: `mcp-dashboard` all three requirements (removed-gone/unknown-tool-fails/no-removed-endpoint; survivors-unchanged/no-scope-migration/no-movements-tool; README lists survivors + documents removal).
- [x] G-2.5 Modify `objetivo.md` (same-section discipline only): amend the finance rule with the dated ledger-reintroduction reversal note (manual `PATCH` corrections AND movement transaction; movements section replaces withdrawn flow entries; savings/debts rules withdrawn) + update the charts section to movement-sourced gasto/ingreso; keep the parent dated reversal readable; no unrelated edits. Acceptance: `finance-core-invariants` "Governing Document Reversal Recorded" (all four scenarios).
- [x] G-2.6 Archive-alignment fixes (design gatekeeper, non-blocking for code but binding for `sdd-archive` — recorded here so they are not lost): (1) correct `finance-subscriptions` "Subscription Cycle State" prose from "first monthly occurrence strictly after the pay date" to the implemented `max(pay_date, stored_due)` threshold rule (scenarios are binding, prose follows); (2) align the `finance-core-invariants` "Gated Destructive Migration 0013" path wording from `backend/migrations/0013…` to change-folder-staged + copy-at-gate; (3) reconcile the `finance-movements` REST-contract bullet (foreign account/category → 404) with the implemented and spec-enumerated rule — 404 for the URL-addressed movement id, 422 for referenced foreign body ids (the surviving-route convention + design status-code matrix). Acceptance: specs read consistently with the implementation at archive; no behavior change.

Per-slice verification (S-G): `cd backend && cargo test` (incl. `migration_0013_gate` RED pre-gate) + `cd frontend && pnpm test && pnpm exec tsc --noEmit && pnpm build`; post-gate only (after explicit owner authorization + deploy re-confirmation): copy 0013 into `backend/migrations/`, apply out-of-band (autocommit psql, 0011 convention), flip the gate test to catalog assertions. Rollback boundary: pre-gate — revert the commits (nothing applied, nothing lost); post-0013 — code/shape revertible, ROWS NOT RECOVERABLE by design (standing no-backup decision, same as 0011). This slice MUST NOT merge without the recorded owner authorization.

## Cross-slice guards (apply-time checklist)

- [x] X-1 No new dependency in any manifest/lockfile (`backend/Cargo.toml`/`Cargo.lock`, `frontend/package.json`/lockfile, MCP manifests) — `finance-core-invariants` "No New Dependencies And Local-Only Delivery"; if any slice needs one, STOP and flag.
- [ ] X-2 Every chain link green: `cargo test` + `pnpm test` + `tsc --noEmit` + `pnpm build`; full-suite + visual Playwright sweep (3 viewports, 0 console errors) after the chain, following the `odd/tasks/resumen-finanzas-config.md` evidence pattern.
- [ ] X-3 No live reference to removed names outside removal-documenting specs: grep for `/transactions`, `/transfers`, `/budgets`, `/savings-goals`, `/debts`, `transactions`, `budgets`, `savings_goals`, `savings_goal_movements`, `debts`, `debt_payments`, `transaction_type`, `apply_transaction_to_balance`, `apply_transfer_counterparty`, `update_savings_goal_saved`, `update_debt_pending`, `SavingsForms`, `DebtPayments`, `PendingDebts`, `useDebts`, `useSavingsGoals`, `toDebtRows`, `toSavingsViews`, `list_debts` — `finance-core-invariants` "Removal Sequencing Without Dangling References".
- [x] X-4 S-G gate paperwork: owner authorization recorded at apply time; deploy gate re-confirms intentional data loss before any production apply of 0013.
